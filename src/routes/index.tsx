import { BrowserRouter } from 'react-router-dom';

import { AnimatedRoutes } from './AnimatedRoutes';

export const Routes = () => {
  return (
    <BrowserRouter>
      <AnimatedRoutes />
    </BrowserRouter>
  );
};
